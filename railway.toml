# Railway Configuration for Retail Brain Monorepo
# This file helps Railway understand how to deploy each service

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.api-gateway"

[deploy]
startCommand = "node services/api-gateway/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

